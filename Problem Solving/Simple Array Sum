#include <bits/stdc++.h>

using namespace std;

int main()
{
    int n;
    cin>>n;
    int A[n];
    int i,sum=0;
    for(i=0;i<n;i++)
        cin>>A[i];
    for(i=0;i<n;i++)
        sum+=A[i];
    cout<<sum;
    return 0;
}
